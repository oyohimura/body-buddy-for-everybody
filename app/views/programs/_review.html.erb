<div>
  <% review.rating.times do %>
    <i class="fas fa-star star-yellow"></i>
  <% end %>
  <p class="mb-0"><%= review.comment %></p>
  <p>
  <small><em class="text-muted"><%= distance_of_time_in_words_to_now(review.created_at) %> ago by <%= review.user.first_name %> <%= review.user.last_name %></em></small>
  </p>
  <hr>
</div>
